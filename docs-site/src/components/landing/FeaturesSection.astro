---
const features = [
	{
		title: "Sync everywhere",
		desc: "dotai sync generates the right files for every tool. One command, all targets.",
		frames: [
			"  .ai/  \n   │    \n ┌─┼─┐  \n ▼ ▼ ▼  ",
			"  .ai/  \n   │    \n ┌─┼─┐  \n → → →  ",
			"  .ai/  \n   ║    \n ╔═╬═╗  \n ▼ ▼ ▼  ",
		],
	},
	{
		title: "Import what you have",
		desc: "Already configured Claude Code or Cursor? dotai import converts it to .ai/ format. No starting over.",
		frames: [
			"CLAUDE.md\n   ↓    \n scanner \n   ↓    ",
			" .cursor \n   ↓    \n scanner \n   ↓    ",
			"AGENTS.md\n   ↓    \n scanner \n   ↓    ",
		],
	},
	{
		title: "Your edits are safe",
		desc: "Manually tweaked a generated file? dotai detects it and won't overwrite without asking.",
		frames: [
			"┌──┐ ┌──┐\n│░░│≠│▓▓│\n└──┘ └──┘\n  ⚠ diff ",
			"┌──┐ ┌──┐\n│▒▒│≠│▓▓│\n└──┘ └──┘\n  ⚠ diff ",
			"┌──┐ ┌──┐\n│▓▓│=│▓▓│\n└──┘ └──┘\n  ✓ sync ",
		],
	},
	{
		title: "Single source of truth",
		desc: "Rules, skills, agents, MCP servers, permissions — one .ai/ directory for everything.",
		frames: [
			"┌────────┐\n│ ◉ .ai/ │\n│ ◉ yaml │\n└────────┘",
			"┌────────┐\n│ ◎ .ai/ │\n│ ◎ yaml │\n└────────┘",
			"┌────────┐\n│ ○ .ai/ │\n│ ○ yaml │\n└────────┘",
		],
	},
	{
		title: "Catch mistakes early",
		desc: "dotai check validates your config before syncing. Typos don't make it to your tools.",
		frames: [
			"[✓] yaml  \n[✓] refs  \n[✓] paths \n[✓] valid ",
			"[~] yaml  \n[✓] refs  \n[~] paths \n[~] check ",
			"[✓] yaml  \n[✓] refs  \n[✓] paths \n[✓] valid ",
		],
	},
	{
		title: "Add what you need",
		desc: "Markdown rules, skill folders, agent configs, hooks. Use what's relevant, skip what's not.",
		frames: [
			"+ skill/ \n+ agent  \n+ hook   \n= config ",
			"+ dir/   \n+ mcp    \n+ perm   \n= config ",
			"+ skill/ \n+ agent  \n+ hook   \n= config ",
		],
	},
];
---
<section class="ld-section" id="features">
	<div class="ld-container">
		<div class="ld-reveal" style="text-align:center;margin-bottom:3rem;">
			<p class="ld-section-label">// Features</p>
			<h2 class="ld-section-title ld-features-title">Built to stay out of your way</h2>
		</div>
		<div class="ld-features-grid">
			{features.map((f, i) => (
				<div class={`ld-card ld-feature-card ld-reveal ld-delay-${i < 5 ? i + 1 : 5}`}>
					<pre class="ld-feature-icon" data-frames={JSON.stringify(f.frames)}>{f.frames[0]}</pre>
					<h3>{f.title}</h3>
					<p>{f.desc}</p>
				</div>
			))}
		</div>
	</div>
</section>

<script>
	const icons = document.querySelectorAll<HTMLPreElement>('.ld-feature-icon[data-frames]');
	if (icons.length) {
		let tick = 0;
		setInterval(() => {
			tick++;
			icons.forEach(el => {
				const frames: string[] = JSON.parse(el.dataset.frames || '[]');
				if (frames.length) {
					el.textContent = frames[tick % frames.length];
				}
			});
		}, 400);
	}
</script>
